должна общаться по usb-c
должен быть usb (3.1) хаб
поддержка одновременного нажатия основного блока клавиш
возможность переназначать клавиши
возможность назначать что будет, при нажатии комбинаций клавиш (2/3 клавиши одновременно)
хранить в памяти все настройки, в том числе и макросы
легкое и простое обновление прошивки, при этом настройки слетать не должны
поддержка профилей

будет экран, на котором будут показыны след данные -  температура, влажность, язык системы, время, имя профиля, свободное место в памяти,
возможность отобразать другие данные ,которые будут прилетать с операционной системы. 

должно быть API, которое можно вызывать из OS. 
подсветка клавиш 
при нажатии одной кнопки, остальные меняют назначение
определяться как HID

BT - одна из целей, это беспроводная клавиатура. вторая цель -  показывать уведомления с телефона
nfc reader (для разблокирования пк)
fingerprint scaner